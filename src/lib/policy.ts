/**
 * Current privacy + terms policy version. Bump this string when the
 * /privacy or /terms content changes — every existing logged-in user
 * will then see the re-consent modal on their next visit until they
 * agree again. Format: ISO date the policy was last edited.
 */
export const CURRENT_POLICY_VERSION = '2026-06-23';

/** App-version string we tag on consent rows. */
export const APP_VERSION = '1.0.0';
